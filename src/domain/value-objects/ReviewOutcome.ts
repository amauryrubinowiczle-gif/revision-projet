/**
 * ReviewOutcome — résultat binaire d'une auto-évaluation (question, définition,
 * ou verdict global décidé par l'utilisateur en fin de fiche).
 */
export type ReviewOutcome = "SUCCESS" | "FAILURE";
